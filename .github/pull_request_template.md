## Checklist

- [ ] Commits follow [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) (e.g. `feat:`, `fix:`, `ci:`, `docs:`); commitlint enforces this in CI.
- [ ] `npm run lint`, `npm run typecheck`, and `npm run build` pass locally.
